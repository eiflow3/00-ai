{
  "name": "00-ai",
  "version": "0.1.0",
  "private": true,
  "description": "RAG pipeline project with FastAPI backend",
  "scripts": {
    "app:api": "cd backend && uv run uvicorn app.main:app --reload"
  }
}
