[project]
name = "backend"
version = "0.1.0"
description = "Basic FastAPI HTTP server"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "pinecone>=6.0.0",
    "openai>=3.6.0",
    "anthropic>=1.2.0",
    "sse-starlette>=3.4.8",
]

[tool.uv]
package = false
